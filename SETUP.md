["nodejs.org](https://nodejs.org/)\n- **PostgreSQL 15+** - Download from [postgresql.org](https://www.postgresql.org/download/)\n- **Git** - For cloning the repository (optional)\n\n## Step 1: Install PostgreSQL\n\n### Windows\n1. Download PostgreSQL installer from [postgresql.org](https://www.postgresql.org/download/windows/)\n2. Run the installer and follow the setup wizard\n3. During installation, set a password for the `postgres` user (default: `postgres`)\n4. Make sure to install pgAdmin (optional GUI tool)\n5. Complete the installation\n\n### macOS\n```bash\n# Using Homebrew\nbrew install postgresql@15\nbrew services start postgresql@15\n```\n\n### Linux (Ubuntu/Debian)\n```bash\nsudo apt update\nsudo apt install postgresql postgresql-contrib\nsudo systemctl start postgresql\n```\n\n## Step 2: Create the Database\n\n### Option 1: Using Command Line\n\n**Windows (Command Prompt):**\n```cmd\ncreatedb -U postgres calendar_organizer\n```\n\n**Windows (PowerShell):**\n```powershell\n& \"C:Program FilesPostgreSQL15\bincreatedb.exe", "U postgres calendar_organizer\n```\n\n**macOS/Linux:**\n```bash\ncreatedb calendar_organizer\n```\n\n### Option 2: Using psql Interactive Shell\n\n```bash\n# Connect to PostgreSQL\npsql -U postgres\n\n# Inside psql, run:\nCREATE DATABASE calendar_organizer;\nq\n```\n\n### Option 3: Using pgAdmin (GUI)\n\n1. Open pgAdmin\n2. Connect to your PostgreSQL server\n3. Right-click on \"Databases\" \u2192 \"Create\" \u2192 \"Database\"\n4. Name it `calendar_organizer`\n5. Click \"Save\"\n\n## Step 3: Clone and Install Dependencies\n\n```bash\n# Clone the repository (if not already done)\ngit clone <repository-url>\ncd scheduler\n\n# Install backend dependencies\ncd backend\nnpm install\n\n# Install frontend dependencies\ncd ../frontend\nnpm install\n```\n\n## Step 4: Configure Environment Variables\n\nThe backend already has a `.env` file configured for local development. Verify the settings:\n\n```env\n# backend/.env\nPORT=5000\nNODE_ENV=development\nDATABASE_URL=postgresql://postgres:postgres@localhost:5432/calendar_organizer\nJWT_SECRET=your-super-secret-jwt-key-change-this-in-production\nSENDGRID_API_KEY=SG.test-key-for-development-purposes-only\nSENDGRID_FROM_EMAIL=test@example.com\nANTHROPIC_API_KEY=test-key\nCANVAS_CLIENT_ID=test-client-id\nCANVAS_CLIENT_SECRET=test-client-secret\nCANVAS_REDIRECT_URI=http://localhost:3000/canvas/callback\nFRONTEND_URL=http://localhost:3000\nCORS_ORIGIN=http://localhost:3000\n```\n\n**Important:** Change the `JWT_SECRET` to a secure random string in production.\n\n## Step 5: Run Database Migrations\n\n```bash\ncd backend\nnpm run migrate\n```\n\nThis will create all necessary tables:\n- `users` - User accounts\n- `courses` - Course information\n- `meeting_times` - Course meeting schedules\n- `topics` - Course topics\n- `topic_prerequisites` - Topic dependencies\n- `topic_sessions` - Scheduled topic sessions\n- `assignments` - Course assignments\n- `assignment_topics` - Assignment-topic relationships\n- `disruptions` - Schedule disruptions\n- `schedule_versions` - Schedule version history\n- `change_logs` - Change tracking\n\n## Step 6: Start the Application\n\nYou need to run both backend and frontend in separate terminals.\n\n### Terminal 1: Backend\n\n```bash\ncd backend\nnpm run dev\n```\n\nThe backend will start on `http://localhost:5000`\n\n### Terminal 2: Frontend\n\n```bash\ncd frontend\nnpm run dev\n```\n\nThe frontend will start on `http://localhost:3000` (or another port if 3000 is in use)\n\n## Step 7: Access the Application\n\n1. Open your browser and navigate to `http://localhost:3000`\n2. Register a new account\n3. Start creating courses and managing your academic calendar\n\n## Troubleshooting\n\n### PostgreSQL Connection Issues\n\n**Error: \"Connection refused\" or \"Cannot connect to database\"**\n\n1. Verify PostgreSQL is running:\n   - **Windows:** Check Services \u2192 PostgreSQL\n   - **macOS:** `brew services list`\n   - **Linux:** `sudo systemctl status postgresql`\n\n2. Verify the database exists:\n   ```bash\n   psql -U postgres -l\n   ```\n\n3. Check your `.env` file matches your PostgreSQL credentials:\n   - Default user: `postgres`\n   - Default password: `postgres` (or what you set during installation)\n   - Default port: `5432`\n\n### Port Already in Use\n\n**Error: \"Port 5000 is already in use\"**\n\nChange the backend port in `backend/.env`:\n```env\nPORT=5001\n```\n\n**Error: \"Port 3000 is already in use", "Vite will automatically use the next available port (3001, 3002, etc.)\n\n### Migration Errors\n\n**Error: \"relation already exists\"**\n\nThis is normal - the migration script uses `CREATE TABLE IF NOT EXISTS`\n\n**Error: \"extension 'uuid-ossp' already exists\"**\n\nThis is also normal - the script checks before creating\n\n### Node.js Version Issues\n\n**Error: \"Module not found\" or other dependency errors**\n\n1. Verify Node.js version:\n   ```bash\n   node --version  # Should be 18+\n   npm --version\n   ```\n\n2. Clear npm cache and reinstall:\n   ```bash\n   cd backend\n   rm -rf node_modules package-lock.json\n   npm install\n   \n   cd ../frontend\n   rm -rf node_modules package-lock.json\n   npm install\n   ```\n\n## Development Workflow\n\n### Running Tests\n\n```bash\n# Backend tests\ncd backend\nnpm test\n\n# Frontend tests\ncd frontend\nnpm test\n```\n\n### Building for Production\n\n```bash\n# Build frontend\ncd frontend\nnpm run build\n\n# Start backend in production mode\ncd ../backend\nnpm start\n```\n\n### Resetting the Database\n\n**Warning: This will delete all data!**\n\n```bash\n# Drop and recreate database\npsql -U postgres\nDROP DATABASE calendar_organizer;\nCREATE DATABASE calendar_organizer;\nq\n\n# Run migrations again\ncd backend\nnpm run migrate\n```\n\n## Useful Commands\n\n### PostgreSQL\n\n```bash\n# Connect to database\npsql -U postgres -d calendar_organizer\n\n# List all databases\npsql -U postgres -l\n\n# Backup database\npg_dump -U postgres calendar_organizer > backup.sql\n\n# Restore database\npsql -U postgres calendar_organizer < backup.sql\n\n# View tables\npsql -U postgres -d calendar_organizer -c \"dt", "Node.js\n\n```bash\n# Check Node.js version\nnode --version\n\n# Check npm version\nnpm --version\n\n# Clear npm cache\nnpm cache clean --force\n\n# Update dependencies\nnpm update\n```\n\n## Next Steps\n\n- Review the [API Documentation](README.md#api-documentation)\n- Explore the [Project Structure](README.md#project-structure)\n- Check out the [Features](README.md#features)\n\n## Support\n\nIf you encounter issues not covered in this guide:\n1. Check the terminal output for error messages\n2. Verify all prerequisites are installed correctly\n3. Ensure PostgreSQL is running and accessible\n4. Review the environment variables in `.env`"]