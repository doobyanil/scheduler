["backend/.env`](backend/.env:3)\n   - Or manually edit [`backend/.env`](backend/.env:3) and update the password\n\n## Method 2: Using Command Line (If you know current password)\n\nIf you know your current password and just want to change it:\n\n```cmd\npsql -U postgres\n```\n\nEnter your current password when prompted, then run:\n\n```sql\nALTER USER postgres WITH PASSWORD 'new_password';\nq\n```\n\nReplace `new_password` with your desired password.\n\n## Method 3: Using Reset Script (If you forgot the password)\n\nI've provided a helper script to reset the password:\n\n```cmd\n# Run as Administrator\nreset-postgres-password.bat\n```\n\n**Important:** This script must be run as Administrator.\n\n### How to run as Administrator:\n1. Right-click on `reset-postgres-password.bat`\n2. Select \"Run as administrator", "The script will:\n1. Detect your PostgreSQL installation\n2. Stop the PostgreSQL service\n3. Reset the password\n4. Restart the service\n5. Show you the new password\n\n## Method 4: Manual Reset (Advanced)\n\nIf the automated script doesn't work, you can reset manually:\n\n### Step 1: Stop PostgreSQL Service\n\n```cmd\nnet stop postgresql-x64-15\n```\n\n### Step 2: Edit pg_hba.conf\n\n1. Navigate to your PostgreSQL data directory (usually `C:Program FilesPostgreSQL15data`)\n2. Open `pg_hba.conf` in a text editor (Notepad)\n3. Find the line that looks like:\n   ```\n   host    all             all             127.0.0.1/32            scram-sha-256\n   ```\n4. Change `scram-sha-256` to `trust`\n5. Save the file\n\n### Step 3: Start PostgreSQL Service\n\n```cmd\nnet start postgresql-x64-15\n```\n\n### Step 4: Reset Password\n\n```cmd\npsql -U postgres\n```\n\nYou should now be able to connect without a password. Run:\n\n```sql\nALTER USER postgres WITH PASSWORD 'new_password';\nq\n```\n\n### Step 5: Restore pg_hba.conf\n\n1. Open `pg_hba.conf` again\n2. Change `trust` back to `scram-sha-256`\n3. Save the file\n\n### Step 6: Restart PostgreSQL Service\n\n```cmd\nnet stop postgresql-x64-15\nnet start postgresql-x64-15\n```\n\n## Method 5: Reinstall PostgreSQL (Last Resort)\n\nIf nothing else works, you can reinstall PostgreSQL:\n\n1. **Uninstall PostgreSQL**:\n   - Go to Control Panel \u2192 Programs and Features\n   - Find PostgreSQL and uninstall it\n   - **Important:** Choose to keep the data directory if asked\n\n2. **Reinstall PostgreSQL**:\n   - Download from https://www.postgresql.org/download/windows/\n   - Install with your desired password\n\n3. **Update your project**:\n   - Run `update-password.bat` to update [`backend/.env`](backend/.env:3)\n\n## After Resetting the Password\n\nOnce you've reset the password, update your project configuration:\n\n### Option 1: Use the helper script\n```cmd\nupdate-password.bat\n```\n\n### Option 2: Manual update\n1. Open [`backend/.env`](backend/.env:3) in a text editor\n2. Find line 3: `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/calendar_organizer`\n3. Replace the second `postgres` (the password) with your new password\n4. Save the file\n\n## Testing the Connection\n\nAfter updating the password, test the connection:\n\n```cmd\ncd backend\nnpm run migrate\n```\n\nIf this runs successfully, your password is correct!\n\n## Common Issues\n\n### \"Access denied\" when running reset script\n\n**Solution:** Make sure you're running as Administrator:\n- Right-click the script\n- Select \"Run as administrator\"\n\n### \"psql is not recognized", "Solution:** Add PostgreSQL to your PATH:\n1. Press `Win + R`, type `sysdm.cpl`, press Enter\n2. Go to \"Advanced\" \u2192 \"Environment Variables\"\n3. Under \"System variables", "find \"Path\" and click \"Edit\"\n4. Click \"New\" and add: `C:Program FilesPostgreSQL15\bin`\n5. Click \"OK", "on all dialogs\n6. Restart Command Prompt\n\n### Service won't start after reset\n\n**Solution:** Check the PostgreSQL logs:\n1. Open `C:Program FilesPostgreSQL15datalog`\n2. Look at the latest log file for error messages\n3. Common issues:\n   - Port 5432 already in use\n   - Data directory corrupted\n   - Insufficient permissions\n\n## Security Tips\n\n- Use a strong password (at least 12 characters, mix of letters, numbers, and symbols)\n- Don't use the default \"postgres", "password in production\n- Keep your password secure and don't share it\n- Consider using a password manager\n\n## Need Help?\n\nIf you're still having issues:\n1. Check the [POSTGRESQL_SETUP_WINDOWS.md](POSTGRESQL_SETUP_WINDOWS.md) for more troubleshooting\n2. Review PostgreSQL documentation: https://www.postgresql.org/docs/\n3. Check the PostgreSQL logs for error messages"]